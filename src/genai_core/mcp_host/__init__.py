"""Python MCP host scaffolding.

This module provides a minimal MCP-compatible HTTP surface so the platform
can run tool/agent capabilities in-process (Python) while keeping the
Orchestrator agnostic (it only speaks MCP).
"""
